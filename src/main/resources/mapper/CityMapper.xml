<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.kgromov.mappers.CityMapper">
    <resultMap id="CityMapperMap" type="org.kgromov.model.City">
        <id property="id" column="ID" />
        <result property="name" column="Name" />
        <result property="district" column="District" />
        <result property="population" column="Population" />

        <association property="country" javaType="org.kgromov.model.Country">
            <id property="code" column="Code"/>
            <result property="name" column="country_name"/>
        </association>
    </resultMap>

    <resultMap id="CityGroupingMap" type="org.kgromov.model.GroupByName">
        <result property="name" column="Name" />
        <result property="count" column="count" />
    </resultMap>

    <select id="findAll"  resultMap="CityMapperMap">
        SELECT c.*, co.Code, co.Name as country_name
        FROM city c
        JOIN country co ON c.CountryCode = co.Code
        ORDER BY c.Name
    </select>

    <select id="findById" resultMap="CityMapperMap">
        SELECT c.*, co.Code, co.Name as country_name
        FROM city c
        JOIN country co ON c.CountryCode = co.Code
        WHERE ID = #{id}
    </select>

    <select id="findAllByCountryCode" resultMap="CityMapperMap">
        SELECT c.*, co.Name as country_name
        FROM city c
        JOIN country co ON c.CountryCode = co.Code
        WHERE c.CountryCode = #{countryCode}
        ORDER BY c.Name
    </select>

    <select id="findByName" resultMap="CityMapperMap">
        SELECT c.*, co.Code, co.Name as country_name
        FROM city c
        JOIN country co ON c.CountryCode = co.Code
        WHERE c.Name = #{name}
    </select>

    <select id="findAllByNotUniqueByName" resultMap="CityGroupingMap">
        SELECT Name, COUNT(*) as count
        FROM city
        GROUP BY Name
        HAVING COUNT(*) > 1
        ORDER BY COUNT(*) DESC
    </select>

    <select id="findTop10ByNotUniqueByName" resultMap="CityGroupingMap">
        SELECT Name, COUNT(*) as count
        FROM city
        GROUP BY Name
        HAVING COUNT(*) > 1
        ORDER BY COUNT(*) DESC
        LIMIT 10
    </select>
</mapper>